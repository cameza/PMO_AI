# Root requirements.txt — used by Vercel for Python Function deployment
# Keep slim to stay under 250MB bundle limit
# Heavy deps (llama-index, chromadb, sentence-transformers) excluded — 
# RAG moves to Supabase pgvector in production

# Web framework
fastapi==0.109.0

# Data validation
pydantic==2.5.3

# Database (Supabase PostgreSQL)
supabase>=2.0.0
sqlalchemy==2.0.25

# Environment variables
python-dotenv==1.0.0

# LLM providers (temporary — will be replaced by Vercel AI Gateway)
anthropic==0.18.1
openai==1.12.0

# Utilities
httpx==0.26.0
