# Root requirements.txt — used by Vercel for Python Function deployment
# Keep slim to stay under 250MB bundle limit
# LLM calls go through Next.js API route (Vercel AI SDK), not FastAPI
# anthropic, openai, sqlalchemy NOT needed here — only in backend/requirements.txt

# Web framework
fastapi==0.109.0

# Data validation
pydantic==2.5.3

# Database (Supabase PostgreSQL)
supabase==2.4.0

# Environment variables
python-dotenv==1.0.0

# HTTP client (used by rag.py for OpenAI embeddings API)
httpx==0.26.0
