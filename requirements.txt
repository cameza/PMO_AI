# Root requirements.txt — used by Vercel for Python Function deployment
# Keep slim to stay under 250MB bundle limit
# LLM calls go through Next.js API route (Vercel AI SDK), not FastAPI
# supabase-py REMOVED — too heavy (~250MB). Using direct httpx PostgREST calls instead.

# Web framework
fastapi==0.109.0

# Data validation
pydantic==2.5.3

# Environment variables
python-dotenv==1.0.0

# HTTP client (direct PostgREST + OpenAI embedding calls)
httpx==0.25.2
